# Malyarka Clean

Future clean application project.

Do not copy old Malyarka files here blindly.

First MVP:

```text
text order -> parse sizes -> disputed rows -> area -> Corel export -> tests
```


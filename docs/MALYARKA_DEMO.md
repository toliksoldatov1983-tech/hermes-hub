# MALYARKA_DEMO

Use this command:

```cmd
scripts\hermes.cmd malyarka-demo
```

It summarizes:

- synthetic fixtures;
- ready/disputed fixture counts;
- workflow status;
- future export columns;
- export gate status.

The demo uses local synthetic data only.
